<%= form_with(model: course_enrollment) do |form| %>
  <% if course_enrollment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course_enrollment.errors.count, "error") %> prohibited this course_enrollment from being saved:</h2>

      <ul>
        <% course_enrollment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%= form.text_field :student_id %>
  </div>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.text_field :course_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
